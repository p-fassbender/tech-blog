const router = require('express').Router();
const { User, Post, Comment } = require('../../models');

// get all users
// get one user
// create a user
// login user session
// logout user session
// update user
// delete user

module.exports = router;