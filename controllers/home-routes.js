const router = require('express').Router();
const { Post, User, Comment, Vote } = require('../models');

// get all posts to render homepage view
// get one post to render single post view

module.exports = router;